# Eufy Security WS Environment Variables
# Copy this file to .env and fill in your actual values

# ==========================================
# REQUIRED: Eufy Account Credentials
# ==========================================
# Your Eufy Security account email
EUFY_EMAIL=your-email@example.com

# Your Eufy Security account password
EUFY_PASSWORD=your-password

# ==========================================
# OPTIONAL: Server Configuration
# ==========================================
# Port for the WebSocket server (default: 3000)
PORT=3000

# Logging level: error, warn, info, debug (default: info)
LOG_LEVEL=info

# Trusted device name for Eufy server identification
TRUSTED_DEVICE_NAME=eufy-security-ws

# ==========================================
# OPTIONAL: MQTT Integration
# ==========================================
# Enable MQTT support by configuring these variables
# MQTT allows publishing device events to an MQTT broker (e.g., Home Assistant)

# MQTT broker connection URL (e.g., mqtt://192.168.1.50:1883)
# MQTT_BROKER_URL=mqtt://your-mqtt-broker-host:1883

# MQTT authentication credentials
# MQTT_USERNAME=your-mqtt-username
# MQTT_PASSWORD=your-mqtt-password

# MQTT client identifier (default: eufy-security-ws)
# MQTT_CLIENT_ID=eufy-security-ws

# Topic prefix for published messages (default: eufy_security)
# MQTT_PREFIX=eufy_security

# ==========================================
# NOTES
# ==========================================
# 1. Never commit the .env file to version control
# 2. For 2FA-enabled accounts, you may need to disable 2FA temporarily
# 3. Server will be available at ws://localhost:3000 (or the port specified in PORT variable)
# 4. MQTT is optional - the server works perfectly without it
# 5. Push notifications are also optional and rarely needed for Scrypted integration